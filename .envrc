#!/usr/bin/env bash
if ! has devenv 2>/dev/null; then
  echo "devenv is not installed — install via nix: https://devenv.sh/getting-started/"
fi
eval "$(devenv direnvrc)"
use devenv
watch_file devenv.nix
watch_file devenv.yaml
watch_file devenv.lock
